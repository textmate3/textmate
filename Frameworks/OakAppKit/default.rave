target "${dirname}" {
	require OakFoundation bundles crash file io ns parse regexp settings text theme
	headers src/*.h

	files resources/* gfx/CloseButton/*.png "Resources"

	sources src/*.{cc,mm}
	frameworks Cocoa AudioToolbox Quartz UniformTypeIdentifiers
	libraries sqlite3
}
